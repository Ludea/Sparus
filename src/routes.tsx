import Index from "components/Index";
import Options from "components/Options";

const routes = [
  {
    path: "/",
    element: <Index />,
  },
  {
    path: "/options",
    element: <Options />,
  },
];

export default routes;
